<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/output.css">
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.4.1/dist/flowbite.min.css" rel="stylesheet" />
</head>
<body class="bg-gray-200 animate-fade">

    <style>
        
    </style>
    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold mb-4">Daftar Pokemon</h1>
        <p class="text-2xl mb-3">Buat yang pengen liat-liat pokemon </p>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 text-center">
            <% pokemonData.forEach(pokemon => { %>
                <div class="bg-white shadow-md rounded-lg overflow-hidden">
                    <img src="<%= pokemon.image %>" alt="<%= pokemon.name %>" class="w-36 h-36 object-cover mx-auto">
                    <div class="p-4">
                        <h2 class="text-xl font-semibold mb-2"><%= pokemon.name %></h2>
                        <small><%= pokemon.description %></small>
                        <br>
                        <hr class="mt-2 mb-2">
                        <a href="/pokemon/<%= pokemon.id %>" class="text-blue-500 hover:text-blue-700">Details</a>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
    

    <script src="https://cdn.jsdelivr.net/npm/flowbite@2.4.1/dist/flowbite.min.js"></script>
</body>
</html>
